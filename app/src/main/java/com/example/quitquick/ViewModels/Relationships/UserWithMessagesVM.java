package com.example.quitquick.ViewModels.Relationships;

import android.app.Application;

import androidx.annotation.NonNull;
import androidx.lifecycle.AndroidViewModel;

import org.jetbrains.annotations.NotNull;

public class UserWithMessagesVM extends AndroidViewModel {
    public UserWithMessagesVM(@NonNull @NotNull Application application) {
        super(application);
    }
}
